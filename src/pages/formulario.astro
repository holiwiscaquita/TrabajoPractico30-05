---
import Layout from "../layouts/Layout.astro";
---

<Layout title="contacto">
    <body>
        <button
            type="button"
            class="text-white bg-blue-700 hover:bg-blue-800 focus:outline-none focus:ring-4 focus:ring-blue-300 font-medium rounded-full text-sm px-5 py-2.5 text-center mr-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800"
            >Default</button
        >
        <main class="flex flex-col items-center" <h1>
            Pagina para encontrar mis redes sociales:

            <nav id="redes">
                <!-- sección de una página cuyo propósito es proporcionar enlaces de navegación, ya sea dentro del documento actual o a otros documentos.-->
                <a href="https://instagram.com"
                    ><img src="instagram.png" width="100" height="100" /></a
                >
                <!--esta combinacion sirve para incluir imagenes las cuales al hacer click sobre ellas, nos enviara al link incluido en href-->
                <a href="https://youtube.com"
                    ><img src="youtube.png" width="100" height="100" /></a
                >
                <a href="https://twitter"
                    ><img src="twitter.png" width="100" height="100" /></a
                >
            </nav>
            <h2>Tambien me podes contactar mediante un mensaje:</h2>
            <form
                action="https://formsubmit.co/alvam307@gmail.com"
                method="POST"
                class="columna"
                id="contacto"
            >
                <div class="fila">
                    <div class="columna">
                        <label for="#nombre">Nombre</label>
                        <input type="text" name="nombre" />
                    </div>
                    <div class="columna">
                        <label for="#apellido">Apellido</label>
                        <input type="text" name="apellido" />
                    </div>
                </div>
                <div class="columna">
                    <label for="#mail">Mail</label>
                    <input type="text" name="mail" />
                </div>
                <div class="columna">
                    <label for="#mensaje">Mensaje</label>
                    <input type="text" name="mensaje" />
                </div>
                <button
                    value="Enviar"
                    class="bg-orange-400 p-2 my-2 w-20"
                    type="submit"
                >
                    Enviar</button
                >
            </form>
            <dialog id="popupFallo">
                Complete los campos
                <button
                    id="cancelFallo"
                    type="reset"
                    value="cerrar"
                    class="text-white bg-blue-700 hover:bg-blue-800 focus:outline-none focus:ring-4 focus:ring-blue-300 font-medium rounded-full text-sm px-5 py-2.5 text-center mr-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800"
                    >X</button
                >
            </dialog>
            <div popover id="popover">Mensaje Exitoso</div>
        </main>
    </body>
    <style>
        .fila {
            display: flex;
            flex-direction: row;
            gap: 24px;
        }

        .columna {
            display: flex; /* permite que sea flexible el contenedor */
            flex-direction: column; /* hace que fluya en direccion de columna */
            gap: 8px; /* separa cada columna de la otra */

            width: 100%;
        }
        #contacto {
            max-width: 480px;
            padding: 40px;
        }

        body {
            background-color: gray; /*configura el color del fondo de esta pagina*/
        }
        h1 {
            font-size: medium;
            text-align: center; /*alinea el texto dentro del elemento*/
            font-style: italic;
        }
        #redes {
            display: flex; /*determina la forma en  la que el/los elemento(s) se presentan en la pantalla*/
            justify-content: space-between; /*Alinea elementos flexibles a lo largo del eje principal de la línea actual del contenedor flexible*/
            margin: 100px;
        }
        h2 {
            font-size: medium;
            text-align: center; /*alinea el texto dentro del elemento*/
            font-style: italic;
        }
        .mailmsj {
            text-align: center;
        }
        .nomape {
            text-align: center;
        }
    </style>
</Layout>
